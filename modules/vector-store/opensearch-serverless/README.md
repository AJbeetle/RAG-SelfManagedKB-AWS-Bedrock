# OpenSearch Serverless Vector Store Module

Creates an Amazon OpenSearch Serverless collection configured for vector search, along with necessary encryption, network, and data access policies.

Outputs conform to the shared vector-store contract required by the `knowledge-base-core` module.
